{
  "name": "test_git",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:main": "cd packages/child_main && pnpm run build",
    "build:test": "git -v && git submodule update --remote && cd packages/child_test && ls -a && pnpm run build && mv dist ../"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
